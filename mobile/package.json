{
  "name": "asr-mvp-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "copy:web": "rm -rf www && mkdir -p www && cp ../web/index.html www/index.html",
    "android:add": "npx cap add android",
    "android:sync": "npm run copy:web && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "typescript": "^5.6.0"
  }
}
