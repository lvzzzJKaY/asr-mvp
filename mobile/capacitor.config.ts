import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zzz.asrmvp',
  appName: 'Cloud ASR MVP',
  webDir: 'www',
  server: {
    cleartext: true
  }
};

export default config;
